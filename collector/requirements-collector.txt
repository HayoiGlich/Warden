# Зависимости импортёра коллектора (Windows). Читает события через PowerShell
# Get-WinEvent, поэтому pywin32/python-evtx НЕ нужны — только запись в Postgres.
sqlalchemy==2.0.45
greenlet==3.3.0
asyncpg==0.31.0
python-dotenv==1.2.1
